name: Auto Update Netflix Data

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run update pipeline
        run: |
          python scripts/run_update_pipeline.py

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

      # ---------------------------------------------------------
      # BACKUP PREVIOUS CSV
      # ---------------------------------------------------------
      - name: Backup previous CSV
        run: |
          if [ -f netflix.csv ]; then
            cp netflix.csv netflix_backup_$(date +%Y%m%d_%H%M%S).csv
          fi

      # ---------------------------------------------------------
      # PULL LATEST CHANGES (avoid fetch-first errors)
      # ---------------------------------------------------------
      - name: Pull latest changes with rebase
        run: |
          git pull --rebase origin main || true

      # ---------------------------------------------------------
      # AUTO-RESOLVE SIMPLE CONFLICTS
      # ---------------------------------------------------------
      - name: Auto-resolve conflicts (keep ours)
        run: |
          if git ls-files -u | grep -q .; then
            echo "Conflicts detected — auto-resolving by keeping our version"
            git checkout --ours .
            git add .
          fi

      # ---------------------------------------------------------
      # SHOW DIFF
      # ---------------------------------------------------------
      - name: Show diff of changes
        run: |
          echo "===== DIFF START ====="
          git diff || true
          echo "===== DIFF END ====="

      # ---------------------------------------------------------
      # COMMIT CHANGES
      # ---------------------------------------------------------
      - name: Commit changes
        run: |
          if [ -n "$(git.status --porcelain)" ]; then
            git add .
            git.commit -m "Automated data update"
          else
            echo "No changes to commit"
          fi

      # ---------------------------------------------------------
      # PUSH CHANGES
      # ---------------------------------------------------------
      - name: Push changes
        run: |
          if [ -n "$(git log origin/main..HEAD)" ]; then
            git push origin main
          else
            echo "Nothing new to push"
          fi

